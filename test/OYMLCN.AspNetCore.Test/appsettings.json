{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "jwt": {
    //"name": "access_token",   // AccessToken名称 [默认：access_token]
    "secret": "yourJsonWebTokenScrect",
    "issuer": "oyml.cn",
    "audience": "WebApi"
    //"accessExpiration": 30,   // Token有效期（单位：分钟）[默认：30]
    //"refreshExpiration": 60,  // Refresh有效期（单位：分钟）[默认：60]
    //"clockSkew": 300          // 时钟漂移（允许的时间误差）（单位：秒）[默认：300]
  }
}
